import { notFound } from "next/navigation";

// Corporate page is hidden until further notice.
// To re-enable: restore the full page implementation and remove this file.
export default function CorporatePage() {
  return notFound();
}
